class GoogleConfig {
  GoogleConfig._();

  // The auto-generated "Web client" OAuth client id from android/app/google-services.json
  // (client_type: 3). Not a secret — OAuth client ids are meant to be embedded
  // client-side. Passed as serverClientId so google_sign_in requests an
  // ID token whose audience matches what Firebase Auth expects.
  static const webClientId = '888954163046-8l862bl3t4iep91mq37b56fi9l40i294.apps.googleusercontent.com';
}
