name: finbrief
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  firebase_core: ^4.13.0
  firebase_auth: ^6.5.7
  # No cloud_firestore: the app never touches Firestore directly — every read
  # and write goes through the backend API, which uses the Admin SDK.
  firebase_messaging: ^16.5.0
  firebase_crashlytics: ^5.2.7
  firebase_analytics: ^12.4.6
  provider: ^6.1.5+1
  http: ^1.6.0
  google_mobile_ads: ^9.0.0
  in_app_purchase: ^3.3.0
  shared_preferences: ^2.5.5
  audioplayers: ^6.8.1
  google_fonts: ^8.2.1
  shimmer: ^3.0.0
  google_sign_in: 6.2.1
  share_plus: ^13.3.0
  connectivity_plus: ^7.3.1
  flutter_markdown_plus: ^1.0.12
  url_launcher: ^6.3.2
  cached_network_image: ^3.4.1
  flutter_tts: ^4.2.5
  # Runtime dependency, not just a generator: preserve()/remove() hold the
  # native splash up until the first real screen is ready.
  flutter_native_splash: ^2.4.6

dependency_overrides:
  # Newer path_provider_android/foundation releases pull in jni/objective_c,
  # which use Dart's native-assets "hooks" build step. That step breaks on
  # this machine because the Flutter SDK lives at a path with a space
  # (D:\new\New folder\src\flutter) — pinning to pre-hooks versions works
  # around it. Safe to remove once the SDK is reinstalled to a space-free path.
  path_provider_android: 2.2.17
  path_provider_foundation: 2.4.1
  shared_preferences_android: 2.4.10
  shared_preferences_foundation: 2.5.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4

# App icon. Regenerate the platform icons with: dart run flutter_launcher_icons
flutter_launcher_icons:
  image_path: assets/icon/icon.png
  android: true
  # Android is the shipping platform; the iOS project isn't scaffolded, and
  # enabling this fails on the missing Assets.xcassets. Flip to true if iOS is
  # ever added.
  ios: false

  # Adaptive icons: launchers crop the foreground to a circle, so the art is
  # inset to keep the "F" and the arrow inside the safe zone. The background is
  # the mid-point of the logo's own purple gradient (sampled from the artwork:
  # #9226F6 top-left to #210387 bottom-right), so the ring around the cropped
  # foreground blends with it instead of forming a visible border.
  adaptive_icon_background: "#5C12CE"
  adaptive_icon_foreground: assets/icon/icon.png
  adaptive_icon_foreground_inset: 20

# Launch screen. Regenerate with: dart run flutter_native_splash:create
flutter_native_splash:
  color: "#5C12CE"
  color_dark: "#210387"
  image: assets/icon/icon.png
  image_dark: assets/icon/icon.png
  android_12:
    color: "#5C12CE"
    color_dark: "#210387"
    image: assets/icon/icon.png
    image_dark: assets/icon/icon.png

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    # Legal copy lives in backend/legal so the hosted pages (served by the
    # backend at /privacy and /terms) and these in-app screens render from the
    # same source. Two copies of legal text would eventually disagree.
    - backend/legal/
    # Same artwork the launcher icon and splash are generated from, so the
    # in-app logo can't drift from the one on the store listing.
    - assets/icon/

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
